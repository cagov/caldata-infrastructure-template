name: ci
channels:
  - conda-forge
dependencies:
{% if dbt_target == 'BigQuery' %}
  - dbt-core~=1.4
  - dbt-bigquery=1.4
{% elif dbt_target == 'Snowflake' %}
{{ 0/0 }}  # Intentionally error until we implement Snowflake
{% endif %}
